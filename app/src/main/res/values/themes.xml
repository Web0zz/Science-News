<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.ScienceNews" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/light_blue_50</item>
        <item name="colorPrimaryVariant">@color/light_blue_200</item>
        <item name="colorOnPrimary">@color/dark_background</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/dark_background</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.ScienceNews.AppWidgetContainerParent" parent="@android:style/Theme.DeviceDefault">
        <item name="appWidgetRadius">8dp</item>
        <item name="appWidgetPadding">4dp</item>
        <item name="appWidgetInnerRadius">4dp</item>
    </style>

    <style name="Theme.ScienceNews.AppWidgetContainer" parent="Theme.ScienceNews.AppWidgetContainerParent" />
</resources>